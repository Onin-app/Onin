{
  "name": "{{PLUGIN_NAME}}",
  "version": "1.0.0",
  "description": "{{PLUGIN_DESCRIPTION}}",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "clean": "rimraf dist",
    "test": "vitest run",
    "test:watch": "vitest",
    "package": "node ../../scripts/build-plugin.js package {{PLUGIN_NAME}}"
  },
  "keywords": [
    "baize",
    "plugin",
    "{{PLUGIN_NAME}}"
  ],
  "author": "{{AUTHOR_NAME}}",
  "license": "MIT",
  "dependencies": {
    "@baize/plugin-sdk": "workspace:*"
  },
  "devDependencies": {
    "typescript": "~5.8.3",
    "rimraf": "^6.0.1",
    "vitest": "^2.1.8"
  }
}